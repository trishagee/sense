configure(subprojects.findAll { it.name != 'util' }) {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    task wrapper(type: Wrapper) {
        gradleVersion = '2.2.1'
    }
}